# Deprecated: replaced by daily-profile.yml
# Kept disabled so old schedules stop running after merge.
name: Generate GitHub Stats (deprecated)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use Daily Profile Refresh workflow instead."
